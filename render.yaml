services:
  - type: web
    name: lilian-dorjo-reviews
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_USER
        value: brunofidget@gmail.com
      - key: EMAIL_PASSWORD
        value: okqpnkvcaaflawwd
      - key: FROM_NAME
        value: Lilian Dorjo
